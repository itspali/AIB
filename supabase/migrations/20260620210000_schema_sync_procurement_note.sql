-- Schema sync note: procurement promo engine tables documented in migration 20260620200000.
-- Run `supabase db push` to apply all procurement migrations including:
-- document_posting_runs, inventory_valuation_audit_log, promotional_batches,
-- promo_fulfillment_entitlements, promo_inventory_balances, goods_receipt_landed_charges,
-- purchase_invoice_receipts, vendor_advance_payments, goods_in_transit_vouchers.

SELECT 1;
